PORT=3000
LOG_LEVEL=info

BRIDGE_API_KEY=replace-with-long-random-key

ACTUAL_SERVER_URL=https://actual.example.com
ACTUAL_PASSWORD=service-account-password
ACTUAL_FILE_PASSWORD=

# auto: use Actual account-access budget discovery first
# configured: only use ENTRYSERVER_BUDGETS_JSON
ENTRYSERVER_BUDGET_DISCOVERY_MODE=auto

# optional fallback list for auto mode, required for configured mode
# ENTRYSERVER_BUDGETS_JSON=[{"id":"budget_abc","name":"Main Budget"}]
ENTRYSERVER_BUDGETS_JSON=

ENTRYSERVER_LOCK_TIMEOUT_MS=5000
ENTRYSERVER_REQUEST_TIMEOUT_MS=15000
ENTRYSERVER_BODY_LIMIT_BYTES=1048576

# Actual server should permit service-account password automation:
# ACTUAL_ALLOWED_LOGIN_METHODS=openid,password
# ACTUAL_OPENID_ENFORCE=false
